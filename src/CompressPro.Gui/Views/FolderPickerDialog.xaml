<Window x:Class="CompressPro.Gui.Views.FolderPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择目标目录"
        Width="500" Height="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="PathInput"
                 Grid.Row="0"
                 Margin="0,0,0,8"
                 Text="{Binding SelectedPath, RelativeSource={RelativeSource AncestorType=Window}}"
                 ToolTip="目录路径" />

        <TreeView x:Name="FolderTree"
                  Grid.Row="1"
                  Margin="0,0,0,8"
                  SelectedItemChanged="FolderTree_SelectedItemChanged" />

        <StackPanel Grid.Row="2"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Content="确定" Click="Ok_Click"
                    Width="80" Margin="0,0,8,0" IsDefault="True" />
            <Button Content="取消" Click="Cancel_Click"
                    Width="80" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
